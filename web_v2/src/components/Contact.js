import React from 'react'

const Contact = (props) => {
    // setTimeout(() => {
    //     props.history.push("/about")
    // }, 2000);
    console.log(props)
    return (
        <div className="container">
            <h4 className="center">Contact here</h4>
        </div>
    )
}

export default Contact